Sync list of partipants 

display names of participants 
	order according to queue
	(loop through queue, then total list only taking those not in queue)

add button to take the floor

sync current speaker

add overlay

add button join queue/end talk type

sync queue

add timer
	utc of speaker time end synced in gapi

add over lay for people in the queue?

manager
	check to see if current speaker has app running
	if shift happens, update time

first load 
	check if slybuss
	do anything ot queue? auto join?

change TimeOut stored server side to time remaining in order to fix different computer times
add line return after yellow
run data load on page load

how to send link to app?

sylbuss 
	what if:
		no tabs and slybuss just scrollable onto of queue list and controls

	which slbuss gets loaded? first person w/ url?

63 12
31 24
15 48
7